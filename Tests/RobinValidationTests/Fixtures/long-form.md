# Validation document

This paragraph becomes a typed node.

```swift
let framework = "Robin"
```

| Surface | Status |
| --- | --- |
| Render IR | Typed |
| CSS | Deterministic |

@Footnote(id: "typed-content") {
  Footnotes remain typed content.
}

@Embed(source: "https://www.youtube-nocookie.com/embed/example", title: "Validation video")

<script>alert("raw HTML must be rejected")</script>
