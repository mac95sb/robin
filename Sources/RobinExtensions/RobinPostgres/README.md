# RobinPostgres

RobinPostgres runs RobinData repositories on PostgreSQL using a pooled PostgresNIO connection.

```swift
import RobinPostgres

let database = PostgresDatabase(configuration: configuration)
```

See the RobinPostgres DocC documentation for configuration, migrations, and test databases.
