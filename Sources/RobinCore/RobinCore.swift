/// Shared protocols, diagnostics, environment, and configuration primitives
/// used across Robin's packages.
public enum RobinCore {
  /// The minimum Swift toolchain version required to build Robin.
  public static let minimumSwiftVersion = "6.3.3"
}
